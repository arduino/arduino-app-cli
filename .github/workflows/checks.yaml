name: Run Checks

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# In the same branch only 1 workflow per time can run. In case we're not in the
# main branch we cancel previous running workflow
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read
  # Used by the buf to create a comment with a brief summary of failing tets
  pull-requests: write

jobs:
  run-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dprint/check@v2.2
        with:
          dprint-version: 0.48.0

      - uses: golangci/golangci-lint-action@v8
        with:
<<<<<<< HEAD
          version: v2.11.4
=======
          version: v2.4.0
>>>>>>> 589600d (chore: fix formatting of golangci-lint version in checks.yaml)
          args: --timeout 300s

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Check license headers
        run: |
          go tool task install:addlicense
          go tool task license:check

      - name: Check go mod
        run: |
          go mod tidy
          git diff --color --exit-code
